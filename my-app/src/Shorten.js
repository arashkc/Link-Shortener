import React from 'react';

function Shorten({ inputValue }) {
  // You can use inputValue here to display or process it further
  return (
    <div>
      {/* Example: Displaying the input value */}
      <p>Input Value: {inputValue}</p>
    </div>
  );
}

export default Shorten;
